A class called Histogram is available for your use..  
It’s implementation details are hidden but it’s interface is as follows:

//  Accepts and stores an array of integers.
public void setIntArray(int[] anIntArray) 

// Returns a string containing a histogram of frequencies 
// from the integer array.
public String toString() 
